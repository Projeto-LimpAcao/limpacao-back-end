spring.application.name=limpacao_backend

# MySQL (Windows host via WSL gateway)
spring.datasource.url=jdbc:mysql://172.28.176.1:3306/limpacao?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=limpacao-secret-key-change-in-production-2024
jwt.expiration=86400000
